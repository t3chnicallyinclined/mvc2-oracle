# NOBD console-role dropdown bot — runtime secrets/config for the VPS.
# Uses the NOBD (admin) bot token (needs Manage Roles; its role must sit ABOVE the console roles).
# Copied to discord-bot/roles.env by deploy.sh; then YOU fill the token. chmod 600, gitignored.

DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=1517792642229076070
ROLES_CHANNEL_ID=1519424083215909014
